# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com
# Project Settings > API

# Public URL (safe to expose to the client)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url

# Anonymous Key (safe to expose to the client, used for authentication)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Service Role Key (KEEP SECRET - server-side only, never expose to client)
# Used for admin operations, migrations, and server-side scripts
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
